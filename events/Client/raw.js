/**
 * 
 * @param {import("../../class/ShibaBot")} client 
 * @param {*} data 
 */

module.exports = {
    event: 'raw',
    run: (client, data) => {
        //client.manager.updateVoiceState(data);
    }
};